# =============================================================================
# Deploy to Azure — Container Apps
# =============================================================================
# Triggered manually or after CI passes on main.
# See docs/cicd.md for detailed explanation.

name: Deploy Azure

on:
  workflow_dispatch:
    inputs:
      environment:
        description: 'Target environment'
        required: true
        default: 'dev'
        type: choice
        options:
          - dev
          - stg

permissions:
  id-token: write
  contents: read

env:
  AZURE_REGION: westeurope

jobs:
  deploy:
    name: Deploy to Azure ${{ github.event.inputs.environment }}
    runs-on: ubuntu-latest
    environment: azure-${{ github.event.inputs.environment }}

    steps:
      - uses: actions/checkout@v4

      - name: Login to Azure
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

      - name: Login to ACR
        run: |
          az acr login --name ${{ secrets.ACR_NAME }}

      - name: Build and push Docker image
        run: |
          docker build -t ${{ secrets.ACR_NAME }}.azurecr.io/rag-chatbot:${{ github.sha }} .
          docker push ${{ secrets.ACR_NAME }}.azurecr.io/rag-chatbot:${{ github.sha }}

      - name: Deploy Terraform
        working-directory: infra/azure
        run: |
          terraform init
          terraform apply -auto-approve \
            -var="environment=${{ github.event.inputs.environment }}" \
            -var="image_tag=${{ github.sha }}"

      - name: Update Container App
        run: |
          az containerapp update \
            --name rag-chatbot-${{ github.event.inputs.environment }} \
            --resource-group rag-chatbot-${{ github.event.inputs.environment }}-rg \
            --image ${{ secrets.ACR_NAME }}.azurecr.io/rag-chatbot:${{ github.sha }}
